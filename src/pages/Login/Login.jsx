import React from "react";
import { Layout } from "../../components";

const Login = () => {
    return (
        <Layout>
            <div>Login</div>
        </Layout>
    );
};

export default Login;
