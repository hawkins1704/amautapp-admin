import React from "react";
import { Layout } from "../../components";

const Material = () => {
    return (
        <Layout>
            <div>Material</div>
        </Layout>
    );
};

export default Material;
