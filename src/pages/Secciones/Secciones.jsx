import React from "react";
import { Layout } from "../../components";

const Secciones = () => {
    return (
        <Layout>
            <div>Secciones</div>
        </Layout>
    );
};

export default Secciones;
